<policies>
    <inbound>
        <base />
        <!-- Validate JWT token from Authorization header -->
        <validate-jwt header-name="Authorization"
                     failed-validation-httpcode="401"
                     failed-validation-error-message="Unauthorized. Access token is missing or invalid.">
            <!-- Azure AD configuration -->
            <openid-config url="https://login.microsoftonline.com/{tenant-id}/v2.0/.well-known/openid-configuration" />

            <!-- Expected audience -->
            <audiences>
                <audience>api://your-api-client-id</audience>
            </audiences>

            <!-- Expected issuer -->
            <issuers>
                <issuer>https://sts.windows.net/{tenant-id}/</issuer>
                <issuer>https://login.microsoftonline.com/{tenant-id}/v2.0</issuer>
            </issuers>

            <!-- Required claims -->
            <required-claims>
                <claim name="roles" match="any">
                    <value>User</value>
                    <value>Admin</value>
                </claim>
            </required-claims>
        </validate-jwt>

        <!-- Extract user ID from token and set as variable -->
        <set-variable name="userId" value="@{
            var jwt = context.Request.Headers.GetValueOrDefault("Authorization", "").Replace("Bearer ", "").AsJwt();
            return jwt?.Claims["oid"].FirstOrDefault() ?? jwt?.Claims["sub"].FirstOrDefault();
        }" />

        <!-- Extract user email -->
        <set-variable name="userEmail" value="@{
            var jwt = context.Request.Headers.GetValueOrDefault("Authorization", "").Replace("Bearer ", "").AsJwt();
            return jwt?.Claims["email"].FirstOrDefault() ?? jwt?.Claims["upn"].FirstOrDefault();
        }" />

        <!-- Add user ID to backend request header -->
        <set-header name="X-User-Id" exists-action="override">
            <value>@((string)context.Variables["userId"])</value>
        </set-header>

        <!-- Add user email to backend request header -->
        <set-header name="X-User-Email" exists-action="override">
            <value>@((string)context.Variables["userEmail"])</value>
        </set-header>

        <!-- Remove Authorization header from backend request (optional) -->
        <!-- <set-header name="Authorization" exists-action="delete" /> -->
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
        <!-- Custom error response for authentication failures -->
        <choose>
            <when condition="@(context.LastError.Reason == "Unauthorized")">
                <return-response>
                    <set-status code="401" reason="Unauthorized" />
                    <set-header name="WWW-Authenticate" exists-action="override">
                        <value>Bearer realm="APIM"</value>
                    </set-header>
                    <set-body>@{
                        return new JObject(
                            new JProperty("error", "Unauthorized"),
                            new JProperty("message", "Access token is missing or invalid"),
                            new JProperty("timestamp", DateTime.UtcNow.ToString("o"))
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
        </choose>
    </on-error>
</policies>
