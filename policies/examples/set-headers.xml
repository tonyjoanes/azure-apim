<policies>
    <inbound>
        <base />
        <!-- Add request ID for tracing -->
        <set-variable name="requestId" value="@(Guid.NewGuid().ToString())" />

        <set-header name="X-Request-Id" exists-action="override">
            <value>@((string)context.Variables["requestId"])</value>
        </set-header>

        <!-- Add client IP address -->
        <set-header name="X-Client-IP" exists-action="override">
            <value>@(context.Request.IpAddress)</value>
        </set-header>

        <!-- Add timestamp -->
        <set-header name="X-Request-Time" exists-action="override">
            <value>@(DateTime.UtcNow.ToString("o"))</value>
        </set-header>

        <!-- Add API version -->
        <set-header name="X-API-Version" exists-action="override">
            <value>1.0</value>
        </set-header>

        <!-- Add correlation ID from query or generate new -->
        <set-header name="X-Correlation-Id" exists-action="override">
            <value>@{
                var correlationId = context.Request.Headers.GetValueOrDefault("X-Correlation-Id", "");
                return !string.IsNullOrEmpty(correlationId) ? correlationId : Guid.NewGuid().ToString();
            }</value>
        </set-header>

        <!-- Remove unwanted headers from request -->
        <set-header name="X-Powered-By" exists-action="delete" />
        <set-header name="Server" exists-action="delete" />

        <!-- Set backend authentication header from named value -->
        <set-header name="X-API-Key" exists-action="override">
            <value>{{backend-api-key}}</value>
        </set-header>

        <!-- Transform Authorization header -->
        <set-header name="Authorization" exists-action="override">
            <value>@{
                var authHeader = context.Request.Headers.GetValueOrDefault("Authorization", "");
                if (authHeader.StartsWith("Bearer "))
                {
                    return authHeader; // Keep Bearer token as-is
                }
                else
                {
                    // Convert to Basic auth for backend
                    return "Basic " + Convert.ToBase64String(Encoding.UTF8.GetBytes("{{backend-username}}:{{backend-password}}"));
                }
            }</value>
        </set-header>
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
        <!-- Add response headers -->
        <set-header name="X-Request-Id" exists-action="override">
            <value>@((string)context.Variables["requestId"])</value>
        </set-header>

        <set-header name="X-Response-Time" exists-action="override">
            <value>@(DateTime.UtcNow.ToString("o"))</value>
        </set-header>

        <!-- Add processing time -->
        <set-header name="X-Processing-Time-Ms" exists-action="override">
            <value>@(context.Elapsed.TotalMilliseconds.ToString())</value>
        </set-header>

        <!-- Remove sensitive headers from response -->
        <set-header name="X-AspNet-Version" exists-action="delete" />
        <set-header name="X-Powered-By" exists-action="delete" />
        <set-header name="Server" exists-action="delete" />

        <!-- Add security headers -->
        <set-header name="X-Content-Type-Options" exists-action="override">
            <value>nosniff</value>
        </set-header>

        <set-header name="X-Frame-Options" exists-action="override">
            <value>DENY</value>
        </set-header>

        <set-header name="Strict-Transport-Security" exists-action="override">
            <value>max-age=31536000; includeSubDomains</value>
        </set-header>

        <!-- Add custom branding -->
        <set-header name="X-API-Gateway" exists-action="override">
            <value>Azure APIM</value>
        </set-header>
    </outbound>
    <on-error>
        <base />
        <!-- Add request ID to error response -->
        <set-header name="X-Request-Id" exists-action="override">
            <value>@((string)context.Variables["requestId"])</value>
        </set-header>
    </on-error>
</policies>
